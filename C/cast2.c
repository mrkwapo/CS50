//this program was created to demonstrate 'cast' which is a verb that describes converting one data type to another.
#include <cs50.h>
#include <stdio.h>

int main(void)
{
    int cast1 = 72;
    int cast2 = 73;
    int cast3 = 33;

    printf("%c %c %c\n", (char) cast1, (char) cast2, (char) cast3);
}
